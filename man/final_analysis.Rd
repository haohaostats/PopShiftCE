% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generators_analysis.R
\name{final_analysis}
\alias{final_analysis}
\title{Final analysis and one-sided Wald z-statistic for the marginal estimand}
\usage{
final_analysis(
  df_full,
  pi_target = c("fixed", "asObservedS2"),
  pi_fixed = NULL
)
}
\arguments{
\item{df_full}{A data.frame with columns \code{Y}, \code{T}, \code{Z}, and \code{stage}.}

\item{pi_target}{Target type: \code{"fixed"} or \code{"asObservedS2"}.}

\item{pi_fixed}{Design-fixed prevalence used when \code{pi_target = "fixed"}.}
}
\value{
A list with elements \code{Zf}, \code{Delta_hat}, \code{se}, and \code{pi_hat}.
}
\description{
Fits \code{Y ~ T + Z + T:Z} using OLS and heteroskedasticity-consistent covariance
(HC3 fallback to HC1/HC0/OLS). Supports two target definitions:
}
\details{
\itemize{
\item \code{pi_target = "fixed"}: design-fixed target prevalence \code{pi_fixed}
\item \code{pi_target = "asObservedS2"}: uses the observed Stage-2 prevalence \code{pi_hat}
}

For \code{pi_target = "asObservedS2"}, the variance includes a simple additional
term \code{eta_hat^2 * Var(pi_hat)} with \code{Var(pi_hat) = pi_hat(1-pi_hat)/n_S2}.
This function returns the final-stage test statistic and estimation output;
interpretation of null rejection frequencies (e.g., one-sided Type I error rate
or power) is handled at the simulation-summary level.
}
