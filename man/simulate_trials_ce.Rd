% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_trials_ce}
\alias{simulate_trials_ce}
\title{Simulate many trials and summarize operating characteristics}
\usage{
simulate_trials_ce(
  R,
  n1,
  n2,
  delta,
  piZ,
  theta,
  eta,
  sigmaY,
  error_type = c("normal", "t", "skew"),
  muX_C,
  muX_T = NULL,
  sigmaX,
  gamma0,
  gamma1,
  mu0,
  lookup,
  pi_target = c("fixed", "asObservedS2"),
  pi_fixed = 0.5,
  digits_rate = 3,
  digits_effect = 4,
  digits_asn = 1,
  verbose = FALSE
)
}
\arguments{
\item{R}{Number of trial replicates.}

\item{n1, n2}{Per-arm Stage-1 / Stage-2 sample sizes.}

\item{delta}{Treatment main effect in the \code{Z=0} stratum.}

\item{piZ}{Stage-2 prevalence of \code{Z=1} in the simulation scenario.}

\item{theta}{Main effect of \code{Z}.}

\item{eta}{Treatment-by-\code{Z} interaction.}

\item{sigmaY}{Standard deviation scale of the primary outcome error.}

\item{error_type}{One of \code{"normal"}, \code{"t"}, or \code{"skew"}.}

\item{muX_C}{Stage-1 control-arm surrogate mean.}

\item{muX_T}{Stage-1 treatment-arm surrogate mean. If \code{NULL}, set internally to
\code{muX_C + delta/gamma1}.}

\item{sigmaX}{Standard deviation of the surrogate.}

\item{gamma0, gamma1}{External surrogate projection coefficients.}

\item{mu0}{Baseline intercept for the primary outcome.}

\item{lookup}{A \code{ce_lookup} object from \code{\link[=build_ce_lookup]{build_ce_lookup()}}.}

\item{pi_target}{Target definition for the final analysis: \code{"fixed"} or
\code{"asObservedS2"}.}

\item{pi_fixed}{Design-fixed prevalence used if \code{pi_target = "fixed"}.}

\item{digits_rate}{Decimal places for rate-type outputs in \code{summary_pretty}.}

\item{digits_effect}{Decimal places for effect-type outputs in \code{summary_pretty}.}

\item{digits_asn}{Decimal places for ASN outputs in \code{summary_pretty}.}

\item{verbose}{Logical; if \code{TRUE}, prints periodic progress messages.}
}
\value{
A list with:
\itemize{
\item \code{results}: replicate-level data frame;
\item \code{summary}: numeric one-row summary (including \code{rejection_rate},
\code{early_stop_rate}, coverage, bias/MSE, and ASN);
\item \code{summary_pretty}: fixed-decimal character version of \code{summary}.
}
}
\description{
This is a serial (non-parallel) simulation wrapper designed for robustness
across different user environments. Users can control runtime by adjusting
\code{R}, \code{B_ref}, \code{B_val}, and related Monte Carlo sizes.
}
\details{
In mixed null/alternative settings, the primary rate reported by this function is
\code{rejection_rate} (overall rejection probability, including early stops). Under
null scenarios, this is interpreted as the achieved one-sided Type I error rate;
under alternative scenarios, it is interpreted as power.
}
