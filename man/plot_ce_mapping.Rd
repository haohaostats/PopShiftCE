% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_ce_mapping}
\alias{plot_ce_mapping}
\title{Plot CE mapping diagnostics}
\usage{
plot_ce_mapping(H0_dt, lookup, combine = FALSE, bins = 120)
}
\arguments{
\item{H0_dt}{Data frame with columns \code{z1} and \code{zf}.}

\item{lookup}{A \code{ce_lookup} object from \code{\link[=build_ce_lookup]{build_ce_lookup()}}.}

\item{combine}{Logical; if \code{TRUE}, combine the four panels using \code{patchwork}.}

\item{bins}{Number of bins for the 2D histogram in panel A.}
}
\value{
A named list of \code{ggplot} objects, or a patchwork object if
\code{combine = TRUE}.
}
\description{
Creates CE calibration diagnostic plots from a null sample of \verb{(T1, S_final)}
pairs and a calibrated lookup object. These plots help visualize the calibrated
conditional error mapping and the reference-rule geometry underlying the
targeted one-sided null rejection probability.
}
\details{
By default the function returns a named list of plots:
\itemize{
\item \code{pA}: joint null cloud \verb{(T1, S_final)};
\item \code{pB}: conditional error function \code{e(t1)};
\item \code{pC}: conditional critical value function \code{c(t1)};
\item \code{pD}: marginal density of \code{T1} under \code{H0}.
}

If \code{combine = TRUE}, a patchwork composite (2x2 panel) is returned instead.
}
