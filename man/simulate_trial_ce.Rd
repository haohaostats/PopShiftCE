% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_trial_ce}
\alias{simulate_trial_ce}
\title{Simulate one two-stage trial using a calibrated CE lookup}
\usage{
simulate_trial_ce(
  n1,
  n2,
  delta,
  piZ,
  theta,
  eta,
  sigmaY,
  error_type = c("normal", "t", "skew"),
  muX_C,
  muX_T = NULL,
  sigmaX,
  gamma0,
  gamma1,
  mu0,
  lookup,
  pi_target = c("fixed", "asObservedS2"),
  pi_fixed = 0.5
)
}
\arguments{
\item{n1, n2}{Per-arm Stage-1 / Stage-2 sample sizes.}

\item{delta}{Treatment main effect in the \code{Z=0} stratum.}

\item{piZ}{Stage-2 prevalence of \code{Z=1} in the simulation scenario.}

\item{theta}{Main effect of \code{Z}.}

\item{eta}{Treatment-by-\code{Z} interaction.}

\item{sigmaY}{Standard deviation scale of the primary outcome error.}

\item{error_type}{One of \code{"normal"}, \code{"t"}, or \code{"skew"}.}

\item{muX_C}{Stage-1 control-arm surrogate mean.}

\item{muX_T}{Stage-1 treatment-arm surrogate mean. If \code{NULL}, set internally to
\code{muX_C + delta/gamma1}.}

\item{sigmaX}{Standard deviation of the surrogate.}

\item{gamma0, gamma1}{External surrogate projection coefficients.}

\item{mu0}{Baseline intercept for the primary outcome.}

\item{lookup}{A \code{ce_lookup} object from \code{\link[=build_ce_lookup]{build_ce_lookup()}}.}

\item{pi_target}{Target definition for the final analysis: \code{"fixed"} or
\code{"asObservedS2"}.}

\item{pi_fixed}{Design-fixed prevalence used if \code{pi_target = "fixed"}.}
}
\value{
A list containing decision indicators, estimates, lower bounds, sample
usage, and diagnostic quantities (\code{z1}, \code{zf}, \code{cz1}).
}
\description{
Stage 1 uses a surrogate-only interim. If \code{T1 >= b_ref}, the trial stops for
early efficacy and returns a surrogate-scale one-sided lower confidence limit.
Otherwise, the trial proceeds to Stage 2, analyzes the primary endpoint using
\code{\link[=final_analysis]{final_analysis()}}, and applies the conditional critical value \code{c(T1)}.
}
\details{
For coverage under \code{pi_target = "asObservedS2"}:
\itemize{
\item if the trial proceeds to Stage 2, coverage is assessed against the realized
\code{pi_hat} returned by \code{\link[=final_analysis]{final_analysis()}};
\item if the trial stops early (before Stage 2 is observed), coverage is assessed
against the scenario prevalence \code{piZ}.
}
}
