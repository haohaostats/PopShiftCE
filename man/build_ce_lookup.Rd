% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/h0_ce.R
\name{build_ce_lookup}
\alias{build_ce_lookup}
\title{Build a CE lookup table via H0 Monte Carlo calibration}
\usage{
build_ce_lookup(
  n1,
  n2,
  muX_C,
  sigmaX,
  gamma0,
  gamma1,
  mu0,
  sigmaY,
  theta,
  eta,
  piZ,
  pi_target = c("fixed", "asObservedS2"),
  pi_fixed = 0.5,
  error_type = c("normal", "t", "skew"),
  rho_XY = 0.3,
  alpha_one_sided = 0.05,
  B_ref = 150000,
  batch_size = 5000,
  z1_grid = seq(-3.5, 3, by = 0.05),
  ce_smoother = c("kernel", "binWindow"),
  kernel = c("gaussian", "tricube"),
  h0 = 0.15,
  h_max = 1.5,
  min_neff = 800,
  enforce_monotone = TRUE,
  seed_lookup = 20250901,
  do_validation = TRUE,
  B_val = 50000,
  seed_val = 20250911,
  verbose = TRUE
)
}
\arguments{
\item{n1, n2}{Per-arm Stage-1 / Stage-2 sample sizes.}

\item{muX_C, sigmaX, gamma0, gamma1}{Stage-1 surrogate and projection parameters.}

\item{mu0, sigmaY, theta, eta, piZ}{Primary-outcome and shift parameters.}

\item{pi_target}{Target definition: \code{"fixed"} or \code{"asObservedS2"}.}

\item{pi_fixed}{Design-fixed prevalence used if \code{pi_target = "fixed"}.}

\item{error_type}{Error distribution (\code{"normal"}, \code{"t"}, \code{"skew"}).}

\item{rho_XY}{Stage-1 surrogate-primary dependence used for H0 calibration.}

\item{alpha_one_sided}{Target one-sided significance level for the reference rule calibration.}

\item{B_ref}{Number of null pairs for calibration.}

\item{batch_size}{Batch size used during serial simulation to limit memory use.}

\item{z1_grid}{Grid of interim values where the lookup is estimated.}

\item{ce_smoother}{Either \code{"kernel"} (default) or \code{"binWindow"}.}

\item{kernel}{Kernel for the kernel smoother (\code{"gaussian"} or \code{"tricube"}).}

\item{h0}{Initial bandwidth.}

\item{h_max}{Maximum bandwidth.}

\item{min_neff}{Minimum effective sample size for local smoothing.}

\item{enforce_monotone}{Logical; if \code{TRUE}, enforces monotone \code{e(t1)} and
monotone-decreasing \code{c(t1)} via isotonic regression.}

\item{seed_lookup}{Optional seed used to make the calibration sample
reproducible. Deterministic batch-specific seeds are derived from this value.}

\item{do_validation}{Logical; if \code{TRUE}, runs an independent validation sample.}

\item{B_val}{Number of null pairs for validation if \code{do_validation = TRUE}.}

\item{seed_val}{Optional seed for the validation sample.}

\item{verbose}{Logical; if \code{TRUE}, prints progress summaries.}
}
\value{
An object of class \code{"ce_lookup"} with elements \code{b_ref}, \code{z1_grid},
\code{e_fun}, \code{c_fun}, and \code{meta}.
}
\description{
Calibrates a reference boundary \code{b_ref} for the rule
\code{T1 >= b_ref} or (\code{T1 < b_ref} and \code{S_final >= b_ref}) at a one-sided level
\code{alpha_one_sided}, then estimates the conditional error function \code{e(t1)} and
conditional critical value function \code{c(t1)}.
}
\details{
The default implementation uses kernel-smoothed estimation with adaptive
bandwidths based on a minimum effective sample size and optional monotonicity
constraints (isotonic regression) for numerical stability. A hard-window
fallback (\code{ce_smoother = "binWindow"}) is also available.

Optionally, the function performs an independent null validation step and
stores independent-validation summaries of the achieved one-sided null rejection probability (i.e., achieved one-sided Type I error rate) in \code{lookup$meta$validation}.
}
